# Punchlist

Execution-order ledger. Drains `specs/reviews/1-fable-review.md` (2026-07-01) plus subsequent eval evidence (`0c4d5213` runs 42-45, `49f4645b` runs 24-25) into ordered, dispositioned work. Plans own the detail; this file owns the order. The README table stays the per-plan status index.

Ordering principle: measurement campaigns (Plan 79 baselines, Plan 86 study) are expensive — before spending on them, (a) runs must not be destroyable or pollutable by infrastructure, and (b) the baselined system should already contain the uncontroversial deterministic fixes. Behavior-changing improvements land *after* the baseline, one at a time, measured. Hygiene lands whenever there is slack; it never jumps the queue.

Triage rule for spec↔code divergence (see memory: code is source of truth): each divergence is dispositioned as **code-bug** (fix code), **spec-stale** (fix or annotate docs, low priority), or **decision** (product call needed) — never an automatic "make code match spec".

## Wave 1 — robustness floor (small, deterministic, no review-posture changes)

- [x] **Plan 80** — degrade-and-disclose failure semantics (fable D1, bugs 1/7). Protects every future run, including baseline runs, from one submit failure destroying the review. (2026-07-02)
- [x] **Plan 85** — finalize grace for per-pass timeouts + truthful incomplete labeling. Removes the timeout losses that would inflate Plan-79 variance numbers. Coordinate its outcome-label names with 79's loss attribution. (2026-07-02)
- [x] **Plan 82 (mechanical half)** — no severity demotion on `behaviorChange: "unknown"`; `severityBeforeCap` threaded through suppression guarantees. Bug fix; belongs in the baseline system. Calibration half stays in Wave 4. (2026-07-02)
- [x] **Plan 86 (steps 1-2, 4)** — protocol observability (tool-choice/reasoning recording, eval metric exposure) + provider matrix doc. Zero behavior change; its friction covariates should exist in the baseline telemetry. Step 3 (forcing) deferred to Wave 3; step 5 (study) to Wave 3 end. (2026-07-02)
- [x] **Plan 89, phase A (eval-signal integrity)** — scorer crash on missing artifact (fable bug 13, run 24 evidence), `a...b` target validation (bug 14), coverage under-count + mislabeled safety-coverage source (bug 19), side-less/line-less static-signal binding (bug 17), skip-rules last-match (bug 5). All deterministic; all distort the measurement signal 79 will rely on. (2026-07-02 — Wave 1 complete)

File-conflict note: 80, 85, 82 all touch `verifier.ts`/`lens-runner.ts` — land as a short series in that order, not in parallel. Pre-79 validation is unit/fixture tests, not single eval runs.

## Wave 2 — the instrument, then the baseline

- [x] **Plan 83** — wording-independent fingerprints + compare integrity (fable D7, bug 3). Lands before/with 79: cross-run identity is the harness's regression assertion, and Plan 84 needs stable fingerprints for union-dedupe. (2026-07-02; note: first compare against a pre-83 run shows one-time fingerprint churn)
- [x] **Plan 79** — repeat/recall-rate harness + relay wrong-chain case. The measurement foundation; nothing recall-related gets judged without it after this point. (2026-07-02; relay case at `trails-api/relay-wc`, pinned to `dad51fda`)
- [x] **Plan 90** — token-denominated primary budget: `review.maxBudgetTokens` (renamed from never-set `maxTotalTokens`), default 5,850,000 (25% above largest observed full review). Time demoted to hang-guard in posture. Watch: raise when plan 84's ensemble adds tokens. (2026-07-02)
- [x] **Baseline runs (owner-run)** — COMPLETE (2026-07-02). `49f4645b` runs 29-30 (both pass: one direct-lane, one promotion-rescue) + `0c4d5213` runs 46-47 run simultaneously (46: **5/5 pass — first fully-green run of this case in recorded history, incl. first-ever zero-native match**; 47: 4/5, sole loss erc20 `reviewed-no-candidate`). All four runs `completed_full` with ZERO infrastructure losses (0 dispatch blocks, 0 grace/soft-deadline events, 0 verification incompletes, 1 schema_invalid recovered) — the only loss in the entire baseline is Stage-7 generation variance, exactly Plans 84/81's target. Findings note: `specs/reviews/2-baseline-wave2.md`. Caveat stands: 2 runs/case gives weak rates; Plan 84 validation needs `repeat: N`.
- [x] **Provider serialization follow-up — RESOLVED as provider-side congestion, now lifted (2026-07-03 ~03:15Z).** Probe ladder (temp script, deleted): 4-concurrent one-process tiny / heavy-unique / heavy-shared-prefix / 16k-reservation+thinking / **long-output (4×45s concurrent streams)** — all TTFB 1-2s, fully parallel, minutes after run 47's tail calls still queued at 125-182s. Conclusion: July 1-2 queueing was provider-side load-dependent scheduling (solo requests always fast; an org's 2nd..Nth concurrent heavy requests queued; aggregate scaled sub-linearly with demand — runs 46/47 got ~2 lanes at 8 in-flight). No client-side defect; nothing to fix in codegenie. Permanent watch: per-call `ttfbMs` telemetry; if the pattern returns, escalate to Anthropic with captured request-ids. Wall-clock should return to healthy (~12 min for the big case).
- [x] **maxBudgetTokens re-derivation (plan-90 watch item)** — triggered by run 46's new largest observed review (4,925,828 tokens, within 3.4% of the 6M default's soft-stop); raised to **7,000,000** (owner decision 2026-07-02): ~42% above largest observed, soft-stop 5.95M, headroom for plan 84's ensemble.

## Wave 3 — behavior changes, one at a time, measured against the baseline

- [x] **Plan 76** — anchor rescue at the verification pre-gate (v6). Phase 1 landed 2026-07-02 (`bf8100c`): two-tier reconstruction + anchorSource provenance; identity/publication never trust gate-only anchors. Phase 2 not shipped (stop condition). Eval validation (repeat runs) pending, owner-run.
- [x] **Plan 87** — exact-duplicate clustering landed 2026-07-02 (`41ee308`): fuzzy semantic clusterer deleted (~180 lines); verdicts shared only across exact copies (path+category+hunk/evidence+title+failureMode equality); reject propagation has a per-candidate trail; gate-only representative anchors never create identity. Repeat A/B measurement pending, owner-run.
- [x] **Plan 86 (step 3)** — landed 2026-07-02 (`932ee5a`): Anthropic finalize/repair/no-tool calls run thinking-off + genuinely forced submit; escape hatch `llm.forceSubmitToolChoice` (default true). A/B effect visible in the owner's next eval runs (first-submit validity, schema retries).
- [x] **Plan 91** — COMPLETE 2026-07-03: migration off `/compat` validated end-to-end. A/B gate = run `0c4d5213/50` (protocol posture field-identical to pre-migration); CLI smoke + debugTrace payload spot-check green. Auth Option A (codegenie-owned resolution, per-call apiKey). No compat imports remain; manifest `^0.80.3`.
- [x] **Plan 81 (output-quality slice)** — landed 2026-07-02: promoted candidates carry real predicate titles (no templates), NO fabricated anchors (plan-76 gate-only anchors take over; run-30's published-template-with-fake-anchor shape is structurally impossible), and hunk-scoped evidence. Admission gates unchanged (already implement the three-part rule; promotions are load-bearing per runs 24-30). Classifier/locality/reserve deletions + compensation-lane shrink remain measurement-gated on repeat data; category-from-lens deferred to a hint-schema change (documented in plan).
- [x] **Plan 84** — IMPLEMENTED 2026-07-02, shipped dark (`deepEnsemblePasses` default 1 = off). K independent deep-packet passes, union under plan-87 exact identity, pass-attributed ids/producers, `stage7_ensemble` telemetry. Measurement next: owner-run K=3 vs baseline on both cases (decision table in the plan governs defaults). Wave 3 code COMPLETE.
- [x] **Per-worker session IDs** — stage-scoped session IDs mapped to OpenAI's `prompt_cache_key`, concentrating all Stage-7/9 workers on one cache node above OpenAI's documented ~15 req/min per-key overflow threshold; per-worker IDs (`stage-7-<workerId>`) fix gpt-5.5 cache routing, are inert on direct Anthropic (safe pre-baseline), and avoid gateway affinity pinning. `sessionKeyGranularity: "worker"` recorded in provider_protocol. (2026-07-02)
- [x] **Plan 86 (step 5) — DONE 2026-07-06: findings note at `specs/reviews/3-gpt55-cross-model-study.md`** (two arms; API-key lane parked by owner; verdict: gpt-5.5-codex production-viable at ~2/3 cost, complementarity demonstrated via the owner-confirmed decimals bug). Originally scoped:** Scoped 2026-07-04. Per-worker session keys done (the gpt-5.5 cache-hotspotting handicap is fixed). Prior gpt-5.5 underperformance is now separable hypotheses, each with an instrument: schema friction (first-submit validity + schemaInvalid/repair metrics), tool-call style vs opus-calibrated budgets (toolBudgetRejections, per-packet call counts), forced-choice-with-reasoning API posture (provider_protocol per call; known asymmetry: anthropic runs forced calls thinking-off, openai reasoning-on — document, don't hide), tokenizer/cache-economics differences (token totals move for non-quality reasons), and scorer wording bias (titlePatterns tuned on opus phrasing — manually adjudicate first-run misses before trusting fails, run-49-style).
  - [x] **Pre-study telemetry gate — DONE 2026-07-05:**
    1. ~~Widen `onResponse` capture~~ — now keeps any `*ratelimit*` header + `retry-after` verbatim (provider-agnostic; `request-id`/`x-request-id` were already captured).
    2. ~~Record `reasoningTokens` per call~~ — `normalizeUsage` maps pi `usage.reasoning` (subset of output; undefined = provider doesn't report, never 0-defaulted); threaded through model-calls.jsonl records, per-stage + run summaries, and a `reasoningTokens` eval metric with compare label.
    3. ~~Verify OpenAI `cached_tokens` mapping~~ — VERIFIED in pi 0.80.3 source: `openai-responses-shared.js` maps `input_tokens_details.cached_tokens` → `usage.cacheRead` and `output_tokens_details.reasoning_tokens` → `usage.reasoning`; the codex lane imports the same `processResponsesStream`, so BOTH Responses lanes feed the existing `cacheRead` → `cacheReadTokens` → `providerPromptCacheReadTokens` chain unchanged.
  - [x] **Codex lane support — verified live (smoke run 37 + study runs); third session regime documented in `skills_llm_telemetry.md`. Originally: both `openai` (API key) and `openai-codex` (ChatGPT-plan OAuth) lanes, both on the Responses API.** Verified: pi registers `openai-codex` as a full provider (gpt-5.5 in its catalog, api `openai-codex-responses`) with OAuth in pi's registry, and codegenie's dialect switches already cover `openai-codex-responses` (tool choice, reasoningEffort, protocol fields). Remaining verifications (fold into the smoke phase): (a) `provider login openai-codex` OAuth flow end-to-end via existing CLI machinery; (b) session-key semantics on codex — smoke run 37 shows healthy cache reads (550,912 tokens; 87% of stage-7 input cached) under per-worker keys, so the regime works; still document as the third session regime in the provider matrix (`skills_llm_telemetry.md`); (c) ~~generic header capture~~ done with the telemetry gate 2026-07-05 — confirm on the codex smoke run that the ChatGPT-plan lane actually emits a rate-limit header family.
  - [x] **Smoke runs — codex lane done (run 37); `openai` API-key lane parked by owner decision. Originally:** one cheap gpt-5.5 run of `49f4645b` per lane — `openai` API key, then `openai-codex` OAuth — to catch auth/400s/schema/transport issues before real spend; adjudicate scoring fairness on the first (widen titlePatterns if same-substance findings miss on wording only).
  - [x] **Study protocol — executed as two arms with recorded deviations (K flipped 2->1 mid-study on its own evidence; see findings note). Originally:** opus-4-8 (anthropic key), gpt-5.5 (`openai` key), gpt-5.5 (`openai-codex` OAuth) — the third arm doubles as a serving-lane comparison (API tier vs subscription plan), directly mirroring the July anthropic OAuth-vs-key findings. Both cases, N≥2-3 per arm, identical configs except `llm.provider`/`llm.model` (keep K=2 + adaptiveSecondPass on — mechanism hit-rates per model are a study output); findings note reports recall WITH covariates alongside (first-submit validity, schema repairs, tool volume, cache reads, reasoningTokens, TTFB, cost) so any recall gap ships with its mechanistic explanation.

## Wave 4 — production-path fixes now, calibration when the data lands

Sequencing (2026-07-04): the measurement campaign (owner runs feeding the plan-92/84 decision tables) runs in parallel with code work. Code items below are ordered so nothing landed can muddy an A/B: production-path and hygiene fixes first (eval-independent), calibration items strictly after the rates exist. Current measurement tallies to watch: adaptive hit rate 1-for-4 post-fix (run 53, incl. one expectation rescue), ensemble pass-2 rescues in 3 of 4 ensemble runs, E1 escalation 2-for-2 on aim.

- [x] **Plan 92 (code)** — all three layers landed 2026-07-03 (attention instrument; E1 test-coverage-delta escalator always-on + E2 telemetry-only; adaptive second pass dark behind `review.adaptiveSecondPass`, capped). Measurement pending: owner runs with adaptive enabled in eval cases; per-mechanism attribution via coverageSource/adaptive events keeps combined-run A/Bs readable. Original planning entry: (2026-07-03 from runs 47/49/50: all three losses on normal/default-coverage packets while deep ensembles went [0,0] in 6/8 pass-runs; run 50's missed finding surfaced verbatim as a concrete follow-up hint from the missing packet). Three layers, landed separately: (1) attention-efficiency metrics — zero behavior change, can land immediately; (2) deterministic structural escalators (orphaned test-coverage delta; intent mismatch) — no domain heuristics; (3) adaptive second pass triggered by the pipeline's own near-miss signals, on plan-84 machinery, capped, dark by default. Subsumes the promotion lane's rationale — its success is what earns plan 81's gated deletions.

### Now (eval-independent; land while measurement accumulates)

- [x] **Plan 88** — publisher: reachable summary-only 422 fallback + partial-coverage disclosure in posting body (fable D6, bug 1). Landed 2026-07-04; summary-only fallback is reachable after three inline 422s, final summary rejection is the only fatal fallback path, partial-coverage disclosure uses shared coverage helpers, and deleted-file → LEFT → multiline suspect order is documented and test-pinned.
- [x] **Composition near-duplicate merge (watch → fix, from run 0c4d5213/53)** — two wording-variant routing findings from different ensemble passes published as separate inline findings; stage-9's exact rule correctly kept them separate for independent verification, but stage-10 root-cause grouping should have merged them. Landed 2026-07-04; deterministic Stage-10 grouping now merges evidence-linked cross-file helper/caller root causes transitively, with a guard test proving unrelated findings that merely cite the same helper path stay separate. Still watch: finding-set diversity vs pre-wave run 40 (two extras 53 lacked).
- [x] **Plan 89, phase B (hygiene)** — landed 2026-07-04. Tree-sitter cached trees dispose on eviction and raw-content cache keys hash; stage runtime endpoints are idempotent; crashed run dirs prune by mtime without `run.json`; existing `.codegenie/.gitignore` files are no longer mutated; Go value receivers render `(T).Method`; HTTP status mining is status-context anchored; extra submit calls emit `extra_submit_dropped`; stale PR-ref locks re-check before removal; packet symbol suffix matching requires a segment boundary; related-context omissions carry `hunkIds`; readRange clamps past EOF; eval run ids are minted independently from `telemetry`; README spec links fixed; `--from-artifacts` old-layout replay errors explicitly.

### Data-gated (do NOT start until the decision tables have rates)

- [x] **Plan 82 (calibration half)** — CLOSED 2026-07-04, no severity flapping observed (decision record). Original entry: **(was)** — only if Wave-2/3 data shows severity flapping on `severityAtLeast` expectations; consider scorer matching on `max(severity, severityBeforeCap)`.
- [x] **Plan 74** — COMPLETE 2026-07-04 (`91dd903`): bounded merged-confidence calibration at final composition; confidenceSelections in final-selection.json.
- [x] **Plan 75 (step 1)** — COMPLETE 2026-07-04: adjudicated-reject notes suppressed provenance-exactly; pinning tests landed same day.
- [ ] **Plan 55 (re-scoped)** — docs as intent context; drop the `ReviewDisposition`/`FileRole` machinery per fable §4.
- [x] **Simplification series (fable §6 items 5-8) — FULLY DISPOSITIONED 2026-07-06:** 93 ✓, 94 ✓, 95 ✓ (seam + one scheduler, A/B green runs 41+61), 96 closed-by-census. Details:
  1. **Plan 93** — COMPLETE 2026-07-04: dead ripgrep fast path and per-run ignored-file enumeration deleted; git-grep is the single search engine, with `engine: "git-grep"` retained in telemetry.
  2. **Plan 94** — COMPLETE 2026-07-04: shared util modules landed (`json`, `regex`, `path-roles`, `text-similarity`); `isTestPath` dialects, including coverage escalation's `_test.*` E2-suppression dialect, are explicit named exports; compact vs pretty stable JSON stayed byte-compatible, the dead `lens-runner` follow-up key was deleted, and pinning tests cover the preserved divergences.
  3. **Plan 95** — COMPLETE 2026-07-06 (see plan doc for the full landing record). Earlier status: rung-utilization census complete over `0c4d5213` runs 46-54 + `49f4645b` runs 29-33 (10 schema-invalid calls / 2,436 model calls; Stage-7 cleanup, composer salvage, planner/verifier repair all live). Prompt why ledger landed beside template versions; Stage-7 repair telemetry emitters collapsed to one recorder. Remaining: move Stage-7 cleanup fully behind `recoverInvalidSubmit` and A/B schema-friction flat.
  4. **Plan 96** — CLOSED 2026-07-06 by census (owner decision; adaptive tree stays). Census record: `0c4d5213` runs 46-54 show real mode spread (594 budget events; adaptive selected 211; sliced modes 234; `adaptive_sliced` has the highest rough run+path finding rate). Do not delete blindly; fixed budget must be sized from productive sliced modes and A/B'd before landing.

- [ ] **Ensemble sunset (owner decision, 2026-07-06): K>1 stays opt-in with a deletion clock.** The mechanism's motivating case now passes without it (6 straight K=1 runs, both models), but N=6 is thin for irreversibility and the one shape that still wants K=2 — a critical bug confined to a single packet, no cross-packet redundancy — hasn't appeared in either eval case. Sunset condition: after ~20 more K=1 runs with no single-packet-shape miss that a second pass would plausibly have caught, delete the K>1 pass-planning loop, `deepEnsemblePasses` config, and caps (~100-150 lines). `poolEnsemblePassResults` STAYS regardless — the adaptive second wave runs through it. Tally: post-flip K=1 completions to date = 18/20 (+ runs 41, 42, 62, 43, 63, 44, 64, 65) (runs 38, 39, 40, 41, 42 small; 57, 58, 59, 60, 61, 62 big) with zero losses attributable to the missing second pass; count forward in the measurement log.

- [ ] **Eval diversity — the generalization guard (owner concern, 2026-07-06).** Harness sweep confirms zero eval-domain leakage into model-facing code/prompts (structural rules only, per plan-12 scope note + escalation policy comment), but all tuning is *validated* on one Go/DeFi repo. Add at least one eval case from a different repository and language (TS/Python; different bug classes) so prompt/mechanism changes get a second-repo check before being trusted as general. Until then, treat "works on trails-api" as necessary, not sufficient.
  - **Plan 98 release boundary (begun 2026-07-23):** Rust, Python, and Solidity support develops as one unreleased Phase 1-4 integration series after the standalone PR-head CI prerequisite. The three default-enabled skills will intentionally change the global Stage-5 inventory, `LensRegistry.registryHash()`, and model-call cache identity once at the Phase-5 merge/release gate. Intermediate phase commits are non-comparable development states: do not merge/release them independently and do not establish eval baselines across them. The second-language real-repository eval remains a post-landing owner run, not a substitute for the structural and packed-install gates.
    - **Phase 2 Rust slice implemented (2026-07-23, awaiting review):** trait/impl-aware adapter, deterministic sibling/Cargo-package test links, default-enabled owner-matrix `lang/rust` skill and WHY ledger, structural and fake transport fixtures with a negative control, and packed-skill assertion coexist. This remains an intermediate non-baseline state; no merge, release, or real-model eval baseline is authorized until the Phase-5 cross-language gate.

## Post-decision measurement log (ambient runs)

**Run 0c4d5213/65 (2026-07-06, OPUS K=1, the pre-registered erc20 decision run): PASS — watch item CLOSED as draw variance.** erc20 candidate generated, kept, published ([low/medium] "Balance-increase test refactor drops ERC20/native RPC boundary...") and scored on identical packet inputs to runs 61/64 — the run-64 whiff was reviewer draw variance, exactly like gpt's run-59 whiff; the tests.md trim stands. Full scoreline ties opus's best: 4 matches (routing f1, amountfromusd f1, decimals 7848ccd3-f1 — **opus now 3-for-5 on decimals**, erc20) + guard honored; minFindings satisfied at 4 findings (the run-64 count issue dissolves — normal draws carry 4 families). $22.66 / 52.6m / 246 calls, complete.

**Runs 44 + 64 (2026-07-06, OPUS K=1, first opus pair on revised skills).** Run 44 (small): PASS native f1, $4.86 / 21.3m, complete, zero friction — clean. Run 64 (big): scored FAIL, substance 3/4 — routing (via f2, with adaptive a2f1 backup), amountfromusd native, **decimals converted (opus now 2-for-4)**; zero-native optional-miss; guard honored. Two fail components: (a) **erc20 candidate genuinely absent — opus's first-ever whiff on it** (zero balance/worker candidates); (b) minFindings 4 vs 3 published — but keep-count was 7, merged correctly (5 routing-family keeps -> 1 finding), so the count check punished good merging on a draw that lacked test-family findings. **WATCH ITEM: erc20 whiff coincides with the tests.md trim** (boundary-coverage emphasis reduced 4->3 encodings; safe-pattern dropped) on the more-selective model — but gpt-5.5 runs 43/63 post-trim generated AND scored erc20, so causation is doubtful on n=1. Decision rule: next opus big-case run — erc20 candidate absent again -> restore the tests.md boundary emphasis; present -> draw variance confirmed. Also new this draw: two previously-unreported production families (Sushi txValue parsing, relay price-not-resolved guard removal) reached candidates; 6 rejects at verification.

**Runs 43 + 63 (2026-07-06, gpt-5.5 K=1, first pair on the revised bundled skills): both PASS — skill revision regression-clean.** Run 63: second consecutive scored gpt-5.5 big-case pass; 4 matches + guard (decimals now **6-for-6** on gpt-5.5, erc20 third consecutive scored via widened pattern, amountfromusd native f1); 282 calls / $16.34 / 26.2m, complete, 0 incompletes. Report: 6 findings, all recurring-real families, clean titles. Run 43: PASS native f1, $2.31 / 5.6m, best-in-band; report body includes a concrete worked numeric example. No metric moved on the skill diet; the truncation fix means stage-7 reviewers now receive the full code-review Examples section.

**Runs 42 + 62 (2026-07-06, gpt-5.5 K=1, post-plan-95/p7.10 build): both PASS — including the first scored gpt-5.5 PASS on the big case.** Run 62: 4 matches + guard at 295 calls (under guard), $16.62 / 26.4m, zero schema friction, zero incompletes, 131/131 complete — the artifact fixes compounded (widened erc20 pattern scored `46897188-f1`; p7.10 held; K=1 kept calls in bounds). amountfromusd via adaptive `a81d5adf-a2f1` (adaptive's fourth match-critical rescue). **Decimals expectation converted: gpt-5.5 now 5-for-5 on the owner-confirmed real bug** (`7848ccd3-f1`), opus 1-for-3. Run 42: PASS native f1, $2.69 / 6.3m, all-zero friction. Findings note confirmed on every touched point; no amendments needed beyond the tally.

**Plan 95 seam A/B gate: GREEN (runs 41 + 61, opus K=1, 2026-07-06).** Both PASS, complete, 0 incomplete, 0 failed hunks. The gate got live exercise beyond "flat": run 41 exercised stage-9 model repair (invalid -> repair_attempted -> scheduled -> recovered) and run 61 exercised a stage-10 deterministic salvage through the REWRITTEN generic seam (composer_payload_salvage_succeeded -> schema_invalid_submit_recovered — the exact post-refactor event path), both recovering with recFail 0 and friction at census rates (1 invalid/run). Run 61 is also the best opus scoreline on this case ever: 4 matches including zero-native via adaptive `72a8ab63-a2f1` (adaptive's third match-critical contribution) + erc20 + native amountfromusd; decimals optional expectation not converted by opus this draw (opus 1/3 on it). Retry-path unification (final plan-95 slice) now unblocked.

**Decimals family adjudicated REAL by owner (2026-07-06) — and live on trails-api master (`17e93787`):** `lib/routes/relay/process_quote.go` narrows provider-supplied `Decimals` (int) to `uint8` at 6 sites with the old `validateRelayDecimals` range check gone; repo-wide survey shows 69 decimals-related `uint8(` sites across 11 route files (lz_oft 47). First demonstrated cross-model complementarity: gpt-5.5 reported it 4/4 runs (56-59), opus 1/2 post-K=1 runs (60) and 0/~10 earlier. Product bug report handed to owner; optional-tier expectation added to 0c4d5213 so conversion is tracked. Headline example for the plan-86 findings note.

**Plan 96 CLOSED (owner decision, 2026-07-06):** census falsified the premise; adaptive symbol-context tree stays. The simplification series (93-96) is now fully dispositioned: 93/94 complete, 95 seam landed (retry unification + A/B gate remaining), 96 closed-by-census.

**K=1 counterfactual window opened 2026-07-05 (`0251e8f`, evals repo):** owner skepticism about ensemble value — both cases set to `deepEnsemblePasses: 1` (adaptive stays on). The K=2 decision rested on two pass-2 rescues, both on 49f4645b (opus run 34, gpt-5.5 run 37); 0c4d5213 matches have come from f1/promotion/adaptive. Watch over the next 3-5 runs: if 49f4645b keeps passing via `f1`, ensemble was unneeded redundancy and the decision-record verdict flips to OFF; if it starts coin-flipping on the Hyperlane expectation, the rescues were real and K=2 returns as the eval default. Expect ~15-25% stage-7 spend reduction on the big case either way.

- **Run 0c4d5213/55 (2026-07-04):** PASS 4/4. Nine findings — richest report of the wave era, including BOTH of run 40's unique extras (vacuous-test assertion, LiFi malformed-numeric) → the run-40 breadth question is closed as draw variance, and the stage-10 near-dup merge is NOT over-consolidating (watch item: positive evidence). amountfromusd matched via promotion `u1` — the lane's second zero-decimal rescue since the retention decision. Adaptive post-fix tally: 4 produced / 12 scheduled across runs 53-55. Deep set: 7 ensembled, 3 productive at [1,1]. **Operational finding: the 60-min eval ceiling bound on legitimate work** (61.3 min under congestion TTFB → 10 tail dispatch blocks, 9 verification incompletes, completed_partial — first incomplete-verdict recurrence of the wave era, purely time-tail). Fixed: `maxTimeMinutes` 60→75 both cases (`12515c2`, evals repo) per the hang-guard-not-coverage-bound principle.
- **Run 0c4d5213/60 (2026-07-05, OPUS K=1 big case): PASS 3/3-required** — routing `f1`, amountfromusd native `f1`, erc20 `46897188-f1` (opus generated the candidate as always -> run 59's watch item settled as gpt draw variance, not p7.10 suppression); zero-native optional-unmatched; guard honored. p7.10 regression-clean at scale: 0 incomplete, 0 failed hunks, complete, 0 schema friction. $22.97 / 51.8m / 251 calls / 111.7k reasoning tokens. **Decimals family adjudication upgraded: opus now independently reports it** ([medium/high] "Removed validateRelayDecimals lets out-of-range provider decimals...") — cross-model independent discovery is strong evidence the family is REAL, not a gpt-5.5 FP; human read now confirmatory rather than decisive. Report texture: 4 findings (opus selective) vs gpt-5.5's 7-10 (broad). Arm economics on identical harness/substance: gpt-5.5 $15.89/26.9m vs opus $22.97/51.8m.
- **Run 49f4645b/40 (2026-07-05, OPUS K=1): PASS — ensemble flip confirmed cross-model.** Native `c121cfd3-f1` match with adaptive `a2f1` finding the same bug on the same packet (the trigger-gated insurance visibly in place where blanket K=2 used to be) plus promotion `u1` probing on-chain delivery. $3.99 / 15.4m / 41 calls, complete, zero schema friction, zero incomplete self-reports (p7.10 regression-clean on opus, small case). vs opus K=2 run 35: -$0.79, -8m. Redundancy texture differs by model: opus concentrated (1 packet + adaptive + promotion on it), gpt-5.5 broad (4-5 packets natively); both pass at K=1.
- **Run 0c4d5213/59 (2026-07-05, gpt-5.5, p7.10 validation): PROMPT FIX VALIDATED.** packet_review_incomplete 0 (from 9-10), failed hunks 0 (from 15-17), 131/131 reviewed, completeness COMPLETE — first gpt-5.5 big-case complete — at 276 calls (under guard), $15.89, zero schema friction. amountfromusd matched via adaptive `a81d5adf-a2f1` (adaptive's second match-critical rescue; native f1 also present on the packet). Remaining fail: erc20 candidate genuinely ABSENT this draw (zero balance/worker candidates; first candidate-generation whiff in 4 gpt-5.5 runs — prior 3 all generated it). WATCH: if the erc20 candidate stays absent next run, examine whether p7.10's decisiveness push suppresses borderline testing candidates; single-run evidence says draw variance. Tool rejections 101 — confirmed harmless appetite, coverage now honest.
- **Run 0c4d5213/58 (2026-07-05, gpt-5.5, budgetBoost 1.5 experiment): prediction FALSIFIED — and the diagnosis moved.** Boost verified applied (deep packets 15->23 calls / 5->8 rounds / 32k->48k chars; note info.json `effectiveConfig` does NOT reflect per-case overrides — papercut). Pressure counters unchanged (92 rejections, 76 degraded, 17 failed) while tool executions rose 780->947 and calls 291->369 (broke the 300 guard): gpt-5.5's appetite scales with the ceiling (Parkinson), rejections measure appetite not deficit. Two reframes: (1) **degraded=76 is diff-deterministic** — opus runs 54/55 show the identical 76; never a gpt problem. (2) **failed hunks are self-reported**: `packet_review_incomplete` x9, and the reasons are mostly no-finding conclusions ("no concrete defect was proven") — gpt-5.5 hedges `reviewStatus:"incomplete"` where opus says `no_findings`; only 1/9 was genuine budget exhaustion. Fixed prompt-side: p7.10 defines incomplete strictly (cut-off-before-evaluation only) + WHY-ledger entry. Score: routing PASS, **erc20 PASS via widened pattern** (fix validated), amountfromusd MISS this draw (native f1 absent; promotion u1 fired but verifier rejected on helper-level-evidence grounds — same conversion-variance class as opus's zero-native). Boost verdict: revert to 1.0 — no completeness or recall benefit, +$3.6, +27% calls; provider-conditional budget defaults are NOT justified by this evidence.
- **Run 49f4645b/39 (2026-07-05, gpt-5.5, K=1 counterfactual #2):** PASS via `5b18db96-f1` — FIVE independent first-pass candidates for the Hyperlane bug across five packets, including 9c3e3101-f1 finding it natively (the packet that needed the e2 rescue in run 37). $2.59 / 7.9m / 44 calls, complete, zero schema friction, pressure minimal. **K=1 verdict after 3 runs (38, 39, 57): ensemble contributed nothing that cross-packet redundancy + promotions + adaptive didn't cover; decision-record ensemble verdict FLIPPED to OFF-by-default for evals** (mechanism stays in code as opt-in config). The two historical pass-2 rescues (runs 34/37) were insurance against per-packet draw variance that multi-packet coverage already absorbs on this diff shape.
- **Run 0c4d5213/57 (2026-07-05, gpt-5.5 BIG CASE, K=1):** scored FAIL, substance ~3/4 — identical substance to K=2 run 56, so removing ensemble cost nothing measurable here: routing `f1` PASS, amountfromusd PASS via native `970d0b42-f1` (promotion `u1` probing the same packet as backup), erc20 found/kept/PUBLISHED but title-pattern-missed AGAIN ("Restore provider-backed balance verification coverage") — second consecutive artifact miss, pattern widened in evals repo (`96db0e1`); zero-native miss (optional); LiFi guard honored. K=1 effects vs run 56: calls 342->291 (under the 300 guard), $19.38->$16.40, candidates 24->18 (lost only e2f1 duplicates), report GREW 7->10 findings incl. run-40's two extras (vacuous-test, malformed-LiFi hard-fail) and an adaptive-earned decimals finding (`46695913-a2f1`; 3 adaptive passes). Planner marked 27 deep (gpt-5.5 planner is 4-7x deeper than opus — style, now harmless without pass doubling). Pressure persists at K=1: 94 tool-budget rejections, 76 degraded + 15 failed hunks -> completeness partial — confirms pressure is model-style, not ensemble; budget posture for tool-heavy models stays the #1 harness-fit item. Verification keep 11 / reject 2 / revise 5, zero incompletes; 1 schema invalid repaired.
- **Run 49f4645b/38 (2026-07-05, gpt-5.5, K=1 counterfactual #1):** PASS via native `f1` — and emphatically: FOUR first-pass candidates for the Hyperlane bug from four different packets (incl. 9c3e3101, the packet whose pass 1 was empty in run 37). Cross-packet redundancy substituted for cross-pass redundancy. $2.27 / 5.9m / 38 calls — half run 37's cost, quarter of opus wall-clock; complete; zero schema friction; context pressure collapsed with the ensemble load gone (4 tool-budget rejections vs 25). One draw, but it leans "ensemble unneeded" on this case. Side effect found+fixed: pi codex client's per-session WebSocket cache (5-min idle TTL) kept the CLI process alive ~5 minutes after results printed — `closeProviderTransports()` now closes codex sessions when main() settles (cli/main.ts).
- **Run 0c4d5213/56 (2026-07-05, gpt-5.5 BIG CASE, codex lane):** scored FAIL but substantively ~3/4: routing `f1` PASS, amountfromusd `f1` PASS **natively first-pass** (opus run 55 needed a promotion rescue), zero-native miss (optional; opus usually misses too), LiFi should_not_find guard HONORED (verifier rejected gpt-5.5's candidate on correct grounds). The two scored failures are harness artifacts, not model deficits: (1) erc20 should_find_candidate title-pattern miss — gpt-5.5 found/kept/PUBLISHED the bug as "Balance fallback tests no longer exercise provider/RPC wiring" but the pattern ("ERC20|balanceOf|test coverage|...") is opus-vocabulary-fitted; FIX EVAL PATTERN before the study or gpt arms are systematically penalized. (2) maxModelCalls 342 > 300 — gpt-5.5's planner assigned 18 deep packets (opus norm 3-7) driving call count; the 300 guard was sized on opus (~273). Real covariates: $19.38 / 31m / 4.77M tokens (cheaper+2x faster than opus run 55), 923 tool calls, SEVERE context pressure (156 tool-budget rejections, 76 degraded + 17 FAILED hunks of 131 -> completeness partial) — budgets sized for opus starve gpt-5.5's tool-heavy style; this is the #1 gpt-5.5 harness-fit work item. Schema friction: 2 invalid, both repaired (protocol solid). Mechanisms: adaptive 1 pass, escalations 0, promotions 3 scheduled; stage-5 planner DID reason on the big case (2,852 tokens; zero on small case). 24 candidates with heavy same-root redundancy (10-candidate decimals family) merged to 7 findings incl. a relay/Stargate decimals-narrowing family opus never reported — needs a human eyeball for real-vs-FP.
- **Run 49f4645b/37 (2026-07-05, gpt-5.5 SMOKE, codex lane):** PASS 1/1, complete, $4.44 / 11.5m / 73 calls — first gpt-5.5 pass in project history, on the modern protocol with ZERO schema friction (0 invalid, 0 repairs; the July failure mode is gone). Matched via ensemble `e2f1` (K=2 rescued on gpt-5.5 exactly as on opus run 34); verification keep 6 / revise 2, no rejects/incompletes; final finding [medium/high] with a clean title. One transient WebSocket error, recovered on retry. Provider-behavior covariates vs opus run 35: 2x tool calls (177 vs 72), high context pressure (25 tool-budget rejections, 25 source-budget denials, 5 degraded hunks vs low-single-digit opus norms), less thinking (stage-7 reasoning 59% of output vs 77%; stage 9 33% vs 63%), 2x faster wall-clock. Telemetry: `cached_tokens` -> `providerPromptCacheReadTokens` VERIFIED LIVE (550,912 read tokens); reasoningTokens symmetric. **Gap found: pi's codex transport bypasses `onResponse`** — ttfb/rateLimit/request-id are empty on this lane (1/73 records had ttfb); decide fix-vs-document before the study. Earlier same-day run 36 errored fast+clean on `refresh_token_reused` (codex refresh tokens are single-use rotating; re-login is the only fix; never share a grant between tools).
- **Run 49f4645b/35 (2026-07-05):** PASS, complete, $4.78 / 23.4m / 41 calls — first run on the simplified tree (`bd3fb4e`) and the telemetry gate's first light. Validation: all-git-grep tool lane p50 1ms / p95 93ms (plan 93 clean); attention byCoverage shape matches run 34 with zero escalations both runs (plan 94 clean); schema friction 1 invalid -> 1 repair -> recovered (plan 95 invisible); cost within half a cent of run 34. Expectation matched by native `f1` with ensemble `e2f1` AND promotion `u1` finding the same bug behind it — belt-and-suspenders draw (run 34 needed pass 2; textbook K=2 variance). Zero incomplete verdicts. Telemetry first light: `reasoningTokens` symmetric on Anthropic after all (pi maps `output_tokens_details.thinking_tokens`) — stage 7 spent 77% of output on reasoning, stage 9 63%, stages 5/10 zero; rate-limit headers + request-id per call.
- **Run 49f4645b/34 (2026-07-04):** PASS at K=2 — $4.78 vs K=3's $7.86, same ensemble-pass-2 winning mechanism. K decision validated on first test.

## Decision record (2026-07-04) — measurement rounds concluded

# Decision Record — 2026-07-04 (measurement rounds: runs 32-33, 51-54)

Convened on accumulated rates; no further runs requested by owner.

1. **Ensemble (plan 84): KEEP, opt-in, K=2 everywhere.** Rescue-bearing in most runs (matched candidates from e2 passes in 33/53/54); K=3's third draw unproven (one pass-3 winner, run 32) at 2x small-case cost — 49f4645b set to K=2 (`0197af7` in evals repo). Product default stays 1.
2. **Adaptive second pass (plan 92 L3): KEEP, eval-enabled, no product default.** Post-tightening: 2-for-8 scheduled passes, both hits expectation-critical (a81d5adf x2, fba381a9); 0-for-3 on 72a8ab63. Bounded by flat cap 4.
3. **Promotion lane (plan 81): DELETION OFF THE TABLE.** Head-to-head split: adaptive beat promotion on a81d5adf (run 53), promotion converted zero-native (72a8ab63-u1, run 51) where adaptive whiffed 3 straight. Lane retained in its post-output-quality form; classifier/locality/reserve deletion steps close as not-executed (evidence contradicts).
4. **E1 escalator: unconditional keeper** — 3-for-3 aim, ~zero cost, always-on.
5. **Plan 82 calibration half: CLOSED, no evidence** — severity flapping never materialized across the measurement rounds.
6. **Breadth question (run 40 comparison): resolved as variance, not mechanism cost** — zero-native's packet is reached and hint-noticed every run; conversion is borderline-judgment variance; run 54's 4-merged group verified as one root cause (merge correct).

Unlocked next: plans 74 and 75-step-1 (small calibration code items). Optional spend: plan 86 step 5 study.

## Decisions needed (not code work until decided)

- [ ] **D8 (eval scorer leniency stack)** — either re-spec matching to include the merged view and delete token-fallback/synonym layers, or revert to strict matching. Decide after Wave 2 baselines show how often leniency actually fires.
- [ ] **D10 (static signals) — VERDICT: keep-and-spec both rules (2026-07-06); one code task remains.** Evidence closed the question: `lossy-conversion-before-validation` is the signal class behind the owner-confirmed decimals production bug (gpt-5.5 5-for-5, live on trails-api master), and `test-coverage-delta.ts` feeds the E1 escalator + T2 adaptive trigger and underlies the erc20 expectation both arms convert. Remaining: (a) spec the two rules into the static-signals section (functional spec) with this evidence; (b) fix the last unconditional base-side parse — `deletedExportSignalsForChangedHunk` (`static-signals.ts:307-330`) reads+parses base for every deleted-lines hunk; the sibling changed-export path already has the head-exported gate (`:281-284`) to copy the pattern from.
- [x] **Uncertainty promotion end-state** — DECIDED 2026-07-04: retained (see decision record). Original entry: **(was)** — if Plan-79 repeats show even predicate-only promotions never convert, delete the subsystem (Plan 81 stop-condition governs). EVIDENCE ACCUMULATING: run 53 head-to-head — on the same packet/signal, the promotion was rejected while plan-92's adaptive real draw was kept and matched the expectation. If that pattern holds across the measurement rounds, adaptive replaces promotion and the deletion executes on plan 81's terms.
- [ ] **Planner recovery / safety-deep coverage end-state** — the ~350-line unspec'd subsystem (`planner.ts:295-633`: submit recovery, sparse-plan detection, safety-deep coverage upgrades; fable §2.2) has no simplification plan today. Decide keep-and-spec vs shrink after the Wave-2 baseline shows how often recovery and safety-deep actually fire. (Gap surfaced by codex planning audit, 2026-07-02.)
- [ ] **Intent-signals / behaviorChange capping end-state** — Plan 82 fixes the mechanical `unknown`-demotion bug only; whether the intent-signals + severity-capping subsystem (~250 lines + threading, fable §2.2/D4) shrinks, gets spec'd, or is deleted is decided on Plan-79/82 severity-flap data. (Gap surfaced by codex planning audit, 2026-07-02.)

## Spec-stale ledger (doc notes, no engineering priority — code is source of truth)

From fable §2.2/§2.4, dispositioned as documentation debt, batched into an eventual doc pass: staged artifact layout v2 + manifest; eval `tier`/`llm:`/`expect.*` extensions + fixture materialization; `source {kind:"auto"}` selector; budgetBoost + run-level budget machinery; `maxTimeMinutes` / `--max-time` (2026-07-01); submit-repair ladder design; provider prompt-cache session hints; tool-result cache; adaptive symbol-context budgeting (pending its Wave-4 fate); README spec-path typo; env-var surface note. Divergences where the *code* is wrong are in the waves above instead (D1→80, D3→87, D6→88, bugs→89).

## Drain map (fable §7 → disposition)

| Fable item | Disposition |
| --- | --- |
| P0: failure ladder (D1) | Plan 80, Wave 1 |
| P0: publisher 422 fallback (D6) | Plan 88, Wave 4 |
| P0: fingerprint stability (D7) | Plan 83, Wave 2 |
| P0: severity cap (D4) | Plan 82, Waves 1+4 |
| P0: skip-rules last-match (bug 5) | Plan 89 phase A, Wave 1 |
| P0: scorer crash + `a...b` (bugs 13/14) | Plan 89 phase A, Wave 1 |
| P1: promotion reduction (D2) | Plan 81, Wave 3 |
| P1: pre-clustering exact-only (D3) | Plan 87, Wave 3 |
| P1: human-attention admission (D5) | Plan 75 step 1, Wave 4 |
| P1: shared submit/salvage layer | Plan 95, in progress Wave 4 |
| P1: fixed Stage-6 context budget | Plan 96 census complete; code gated |
| P1: ripgrep fix-or-delete (D9) | Plan 93, complete Wave 4 |
| P1: shared similarity module | Plan 94, complete Wave 4 |
| P2: plan 79 / 76 / 74 / 55 / 75 | Waves 2 / 3 / 4 / 4 / 4 |
| P2: gpt-5.5 study + tool-choice instrumentation | Plan 86, Waves 1+3 |
| P2: Stage-7 ensemble | Plan 84, Wave 3 |
| P3: spec reconciliation | Spec-stale ledger (doc pass) |
| P3: bug sweep (9-20) | Plan 89 phases A/B |
| P3: README paths, eval runId, old-layout replay | 89 phase B; old-layout compare handled in Plan 83 |

New-evidence items with no fable ancestry: Plan 85 (Wave 1, from runs 43/45), `--max-time`/`maxTimeMinutes`/incomplete-review banner (landed 2026-07-01).
