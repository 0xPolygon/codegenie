---
status: complete
---

# Phase 5: Cross-language release gate

## Overview

Validate the complete Plan 98 language series as one atomically release-ready local commit series on top of `eb20533`. This phase runs the repository and consumer-install gates, executes all seven public fixture suites including the Rust/Python/Solidity marker-free negative controls, pins Go/TypeScript Stage-7/9 skill isolation against the real bundled inventory, records a real-model Rust owner smoke when ordinary configured credentials are usable, and synchronizes the normative release-boundary and eval-diversity documentation. It does not publish, tag, push, merge, or claim that an external release occurred: the one external Stage-5 inventory/registry/cache boundary occurs only when the complete local series is later merged or released.

## Steps

1. Add a focused cross-language release test that loads all seven real bundled skills and proves Go and TypeScript Stage-7 and Stage-9 projections retain their existing language guidance while excluding Rust, Python, and Solidity guidance. Assert projection ids, no truncation, and the total projection cap.
2. Copy the exact Plan 98 base revision plus this uncommitted Phase-5 diff into an isolated directory with no `node_modules` or `dist`, prove the CI-pinned pnpm 11 scripts-enabled frozen install, then run `pnpm run check`, `pnpm test`, and `pnpm build` there. Treat the complete Vitest suite as the structural, grammar-lifecycle, language-adapter, packet, likely-test, skill-quality, PR-head workflow, and installed-consumer gate.
3. Run the packed installed-consumer test explicitly. Require the produced package to contain all seven bundled skill Markdown files and all compiled new-language adapters; force and assert `ignore-scripts=false` on the child install while applying an explicit consumer build policy that approves `esbuild` and denies unused native grammar builds; resolve all seven grammar WASMs from the installed dependency tree; minimally parse every grammar; and inspect a semantic Rust, Python, and Solidity symbol through the production registry.
4. Run `pnpm run dev eval --eval-dir evals/fixtures` and record each of the seven suite results. Confirm the Rust, Python, and Solidity marker-free negative controls remain unmatched, while keeping the fake suites scoped to transport and anchoring proof.
5. Run one ordinary non-posting real review on a reproducible Rust fixture revision using the configured provider/model credentials if available. Record the exact fixture base/head SHAs and invocation, expected and inspected enclosing symbol, likely-test path/symbol, `lang/rust` lens and skill, packet/prompt artifact paths, and an evidence-based owner acceptance judgment. If configuration/auth is unusable, finish every automated and documentation gate and return the required roadblock without fabricating the smoke.
6. Update `README.md` and the normative architecture language with the atomic release-unit and future external cache-boundary contract. Claim local release readiness only if every gate, including the owner smoke, is accepted; otherwise record the roadblock without weakening the contract.
7. Update `specs/plans/PUNCHLIST.md` with the Phase-5 automated and owner-smoke evidence, the expected single future Stage-5 inventory/registry/cache boundary, and the fact that Plan 98 broadens structural/fake language coverage but does not satisfy the open real-repository eval-diversity guard. Preserve all Plan 98 deferrals.
8. Record the final exact invocations, artifact locations, observed symbols/tests/lenses/skills, and acceptance judgments in this phase plan. Leave the plan status `draft` and do not toggle, commit, push, merge, tag, publish, or create a release until the manager completes code review and explicitly authorizes the commit step.

## Tests

- `keeps Go and TypeScript Stage 7/9 prompts isolated at the release boundary`: loads the real seven-skill registry, verifies each packet language receives its existing language skill, excludes the three new language skills, and projects without omission or truncation.
- `pnpm install --frozen-lockfile --config.ignore-scripts=false`: from an isolated clean source copy under CI-pinned pnpm 11.15.1, proves the repository's explicit `allowBuilds` policy runs `esbuild` and deterministically denies unused dependency builds without `ERR_PNPM_IGNORED_BUILDS`.
- `pnpm run check`: validates TypeScript and all GitHub workflows with the pinned local `actionlint` requirement.
- `pnpm test`: runs the full structural, lifecycle, adapter, likely-test, packet, skill, fixture-materialization, workflow, and package-consumer test suite.
- `pnpm build`: emits the distributable package and version metadata.
- `pnpm exec vitest run tests/package-build.test.ts`: repeats the real packed installed-consumer gate with explicit script and dependency-build policy for release evidence.
- `pnpm run dev eval --eval-dir evals/fixtures`: runs the core, tests, Go, TypeScript, Rust, Python, and Solidity fake suites and their declared negative expectations.
- Buildable Rust fixture: base `cargo test missing_input_uses_fallback` must pass; feature `cargo check` must pass while feature `cargo test missing_input_uses_fallback` must reproduce the intentional `Option::unwrap()` panic at `src/positive.rs:3`.
- Real Rust review smoke: exercises the ordinary configured provider on a pinned base/head diff and inspects persisted packet and Stage-7/9 prompt artifacts; this is owner product-content acceptance, not an automated shipping substitute.

## Release evidence

### Local series and automated gates

- Authoritative source base: `eb20533` (`feat: add Solidity language support`) plus the uncommitted Phase-5 diff. The preceding local series is `068bc03`, `9360ea5`, `8a32c57`, and `eb20533`, following standalone CI prerequisite `17bbce1`.
- Fresh-install proof: source was copied to `/tmp/codegenie-plan98-fresh-install.ah7CcZ` with `.git`, `node_modules`, `dist`, coverage, run logs, and local telemetry excluded; absence of `node_modules` and `dist` was asserted before installation. Exact invocation `pnpm install --frozen-lockfile --config.ignore-scripts=false` passed under CI-pinned pnpm 11.15.1, installed 177 packages, and ran `esbuild@0.28.1` postinstall successfully. `pnpm ignored-builds` then reported `Automatically ignored builds: None` and explicitly denied `@google/genai`, `protobufjs`, all six grammar packages, and `yarn` through `allowBuilds`; no `ERR_PNPM_IGNORED_BUILDS` occurred.
- Repository/CI policy proof: `pnpm-workspace.yaml` makes pnpm 11 `allowBuilds` canonical, approving only `esbuild` and explicitly denying every unused build listed above. Compatible `onlyBuiltDependencies` / `ignoredBuiltDependencies` keys remain intentionally for pnpm 10 and are test-pinned to the same decisions. `.github/workflows/ci.yml` pins pnpm 11.15.1 and exact install `pnpm install --frozen-lockfile --config.ignore-scripts=false`; workflow contract tests pin both.
- `pnpm run check`: passed in the fresh copy (`tsc -p tsconfig.json --noEmit`; pinned `actionlint` workflow validation).
- `pnpm test`: passed in the fresh copy, 36 test files and 726 tests. This includes all structural language fixtures, parser failure semantics, skill owner matrices, Go/TypeScript regressions, repository/CI build-policy contracts, workflow contracts, fixture materialization, and package tests.
- `pnpm build`: passed in the fresh copy (`tsc -p tsconfig.build.json && node scripts/write-version.mjs`).
- Relevant explicit workflow gates passed in the fresh copy: `pnpm run check:workflows` and `pnpm exec vitest run tests/github-action.test.ts` (40 tests).
- `pnpm exec vitest run tests/package-build.test.ts`: passed in the fresh copy, 4 tests. The exact child install was `pnpm install --offline --prod --no-frozen-lockfile --config.ignore-scripts=false`; the test independently asserted `pnpm config get ignore-scripts --config.ignore-scripts=false` returned `false`. Its generated consumer workspace policy was read back through pnpm and asserted `onlyBuiltDependencies: [esbuild]`, with equivalent pnpm-v11 `allowBuilds` decisions approving `esbuild` and denying the unused native grammar, `@google/genai`, `protobufjs`, and `yarn` builds. Under that policy, the offline installed consumer loaded exactly seven bundled skills, resolved installed WASMs for `go`, `typescript`, `tsx`, `javascript`, `rust`, `python`, and `solidity`, minimally parsed all seven, then inspected Rust impl method `tuple` with owner `(u8, u8)`, Python async method `authorize` with owner `Service`, and Solidity method `withdraw` plus state value `total` with owner `Vault`. No native grammar build is claimed or required; the gate exercises their published WASMs.
- `pnpm exec vitest run tests/language-release-gate.test.ts`: passed. Real bundled projections for Go retained `Goroutine leaks` at Stage 7 and `Passing caller context through unchanged` at Stage 9; TypeScript retained `Floating promises` and `Promise.allSettled`. Both excluded the Rust, Python, and Solidity check/false-positive markers, projected only `core/code-review`, `core/tests`, and the matching language skill, and had zero omission/truncation under the 12,000-character cap.
- `pnpm run dev eval --eval-dir evals/fixtures`: passed with seven cases, zero failures/errors/losses. Final buildable-fixture evidence is `evals/fixtures/logs/43` (core), `44` (Go), `45` (Python), `46` (Rust), `47` (Solidity), `48` (tests), and `49` (TypeScript). Rust/Python/Solidity each satisfied its positive plus marker-free negative expectation.

### Required real-model owner smoke

- Fixture revision: base `51951cf2673a4d905c7f4b3a89f2c499d442dbd2`, head `051440dd2df43a2754a25396a598664f5ea20959`, materialized at `evals/fixtures/logs/46/fixture-repo`. Base `src/lib.rs` wires `positive`, re-exports `positive_marker`, and includes `positive_test` under `#[cfg(test)]`; feature `src/lib.rs` additionally wires the marker-free `negative` module.
- Invocation contract: `node dist/cli/main.js eval --eval-dir /tmp/codegenie-plan98-phase5-smoke`, with non-posting case config pinned to the above head/base, `lang/rust`, verification enabled, cache disabled, debug traces enabled, and explicit provider/model/reasoning.
- Expected context: `src/positive.rs::positive_marker`, declaration range `[1, 4]`, changed reachable `input.unwrap()` at line 3; likely test `src/positive_test.rs::missing_input_uses_fallback`; packet lens and Stage-7/9 skill `lang/rust`. The accepted run's Stage-5 planner dossier inspected the expected function/range and canonical `rust` language.
- Cargo behavior at the pinned revision: on base, exact invocation `cargo test missing_input_uses_fallback` exits 0 with `positive_test::missing_input_uses_fallback ... ok` and `1 passed; 0 failed`. On feature, `cargo check` exits 0, proving the library and wired `negative` module compile; exact invocation `cargo test missing_input_uses_fallback` exits 101 with `called Option::unwrap() on a None value` at `src/positive.rs:3` and `0 passed; 1 failed`.
- Attempt 1: `openai-codex/gpt-5.5:high` failed because OAuth refresh was unusable. Full redacted evidence paths: `/tmp/codegenie-plan98-phase5-smoke/logs/1/info.json`, `/tmp/codegenie-plan98-phase5-smoke/logs/1/telemetry/model-calls.jsonl`, `/tmp/codegenie-plan98-phase5-smoke/logs/1/telemetry/stages/05-planner/planner-dossier.json`, and `/tmp/codegenie-plan98-phase5-smoke/logs/1/telemetry/debug/llm-calls/`.
- Attempt 2: `anthropic/claude-sonnet-4-6:high` failed with `Connection error.` inside the restricted network sandbox. Full redacted evidence paths: `/tmp/codegenie-plan98-phase5-smoke/logs/2/info.json`, `/tmp/codegenie-plan98-phase5-smoke/logs/2/telemetry/model-calls.jsonl`, `/tmp/codegenie-plan98-phase5-smoke/logs/2/telemetry/stages/05-planner/planner-dossier.json`, and `/tmp/codegenie-plan98-phase5-smoke/logs/2/telemetry/debug/llm-calls/`.
- Attempt 3 passed after outside-sandbox authorization at `/tmp/codegenie-plan98-phase5-smoke/logs/3/info.json`, but its fixture did not wire a Cargo target/test module. It is retained as history and superseded by the buildable run 4.
- Attempt 4 used `anthropic/claude-sonnet-4-6:high` through the authorized invocation and passed. Full run evidence: `/tmp/codegenie-plan98-phase5-smoke/logs/4/info.json`; result `1 reported | 2/2 expectations | $0.3064 | 158.4s | complete review | 0 budget overruns`; suite `1 passed, 0 failed, 0 errored`; zero loss counts. Runtime provenance is dirty `eb20533b41c364918636bffb80dd140b0d6a5691` on `next`, with the uncommitted Phase-5 diff.
- Inspected packet: `/tmp/codegenie-plan98-phase5-smoke/logs/4/telemetry/stages/06-packets/packets/e2ff912f2f2237b4158e42c2fb12d00d6e0941222ed2acd8f645f2d62f32cf32.json`. It records `path: src/positive.rs`, `language: rust`, `lenses: [lang/rust]`, `contextQuality: full`, enclosing function and packet symbol `positive_marker` with signature `pub fn positive_marker(input: Option<&str>) -> &str` and range `[1,4]`, changed lines `[2,3]`, and exact relevant test `src/positive_test.rs::missing_input_uses_fallback`, `nativeKind: test case`, range `[3,6]`, signature `#[test] fn missing_input_uses_fallback()`.
- Inspected Stage-7 prompt: `/tmp/codegenie-plan98-phase5-smoke/logs/4/telemetry/debug/llm-calls/mc-000002.request.json`, call `packetReview`, worker `w7-001`, full packet id `e2ff912f2f2237b4158e42c2fb12d00d6e0941222ed2acd8f645f2d62f32cf32`, template `p7.10`, schema `submit_review`. It contains `## Skill: lang/rust`, the full `Reachable panic paths` check, enclosing symbol/context, and exact likely-test path/symbol; it excludes `lang/go`, `lang/typescript`, `lang/python`, and `lang/solidity` skill sections.
- Inspected Stage-9 prompt: `/tmp/codegenie-plan98-phase5-smoke/logs/4/telemetry/debug/llm-calls/mc-000007.request.json`, candidate `e2ff912f-f1`, worker `w9-001`, template `p9.5`, schema `submit_verdict`. It contains only `lang/rust` language guidance, including the compiler-rejected-lifetime false-positive rule, and the exact enclosing/test context; it excludes Go/TypeScript/Python/Solidity skill sections.
- Finding/verification evidence: `/tmp/codegenie-plan98-phase5-smoke/logs/4/telemetry/stages/09-verification/candidate-findings.json` attributes the single high-confidence/high-severity direct candidate to `lang/rust`; `/tmp/codegenie-plan98-phase5-smoke/logs/4/telemetry/stages/09-verification/verification.json` keeps it with required evidence and low false-positive risk; `/tmp/codegenie-plan98-phase5-smoke/logs/4/telemetry/stages/10-composition/final-findings.json` and `/tmp/codegenie-plan98-phase5-smoke/logs/4/telemetry/stages/10-composition/final-selection.json` publish exactly one inline finding at `src/positive.rs:3`; `/tmp/codegenie-plan98-phase5-smoke/logs/4/codegenie-review.out.md` reports the new `negative` module and `lib.rs` wiring as having no identified issue. The marker-free negative expectation produced no final finding.
- Acceptance judgment: **ACCEPTED.** The owner smoke demonstrates a buildable wired crate, an exactly reproducible passing base/failing feature test, precise Rust declaration context, deterministic likely-test linkage, correct language lens/skill isolation, a material skill-guided reachable-panic detection, verifier use of the named test, a clean negative-control outcome, and concise final composition. The complete local series is atomically release-ready for code review. Attempts 1-3 remain fully disclosed and are superseded by the buildable authorized run 4.

### Measurement boundary and eval diversity

No push, PR, merge, tag, publish, remote release, or external Stage-5/cache measurement boundary occurred. When the complete reviewed series is eventually merged/released, its seven-skill inventory establishes one disclosed external `LensRegistry.registryHash()` and model-call-cache boundary. The structural/fake cases and accepted synthetic owner-smoke fixture do not close the PUNCHLIST's second-language real-repository eval-diversity guard; that remains a post-landing owner run.
